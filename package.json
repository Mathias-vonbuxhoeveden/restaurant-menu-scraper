{
  "name": "restaurant-menu-scraper-api",
  "version": "1.0.0",
  "description": "REST API for scraping restaurant menus",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
